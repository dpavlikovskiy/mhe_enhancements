<iscache type="relative" hour="24"/>


<a href="${URLUtils.http('GiftCert-Purchase')}">  
	<!-- [was spacer gif?]  -->
</a>

<isloop items="${slotcontent.content}" var="contentAsset">
	<isif condition="${slotcontent != null}">
		<isprint value="${contentAsset.custom.body}" encoding="off"/>
	</isif>
</isloop>
