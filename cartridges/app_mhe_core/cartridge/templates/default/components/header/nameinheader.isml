 <isif condition="${pdict.CurrentCustomer.registered}">
		  <isprint value="${pdict.CurrentCustomer.profile.title}"/>
     	 <isprint value="${pdict.CurrentCustomer.profile.lastName}"/>
</isif>
 

 